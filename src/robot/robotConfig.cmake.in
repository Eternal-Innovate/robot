# - Config file for the robot package
# It defines the following variables
#  robot_INCLUDE_DIRS - include directories for FooBar
#  robot_LIBRARIES    - libraries to link against
#  robot_EXECUTABLE   - the bar executable

set(robot_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${robot_DIR}/export_robot.cmake")

# These are IMPORTED targets created by robotTargets.cmake


